# Reports

Reports in this directory are generated from JSON state by:

```bash
python .codebase-optimization-kit/kit.py report
```

Do not treat generated reports as source of truth. Regenerate them after state changes.
