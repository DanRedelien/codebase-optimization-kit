# Adapters

Language adapter JSON files describe common manifests, lockfiles, test commands, entrypoint hints, and risk signals.

`kit.py` uses standard-library heuristics first. These files are editable hints for agents and future project-specific refinement.
