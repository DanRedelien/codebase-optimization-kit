# Project Behavioral Parity Policy

Fill this file with project-specific equivalence rules before implementation packets are approved.

Examples:

- APIs: `<snapshot, schema, route, or client contract checks>`
- CLI: `<command output comparison>`
- Data: `<migration, precision, compatibility, or rollback rules>`
- Integrations: `<smoke tests or sandbox checks>`
- Performance: `<thresholds, benchmarks, or resource limits>`
- Errors: `<error type, status code, message, or retry behavior>`

A packet cannot be approved unless compatible inputs, outputs, error behavior, known acceptable differences, and validation commands or manual checks are stated.
